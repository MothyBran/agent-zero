[providers]
node = true
python = true

[phases.setup]
nixPkgs = ["nodejs_22", "npm-10_x", "python3"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

